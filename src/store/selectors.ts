import { selector } from 'recoil'

import { userProfile } from './atoms'

// export const isLoggedIn = selector({
//   key: 'isLoggedIn', // unique ID (with respect to other atoms/selectors)
//   get: ({ get }) => {
//     return Object.keys(get(userProfile)).length
//   },
// })
